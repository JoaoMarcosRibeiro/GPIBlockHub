import { ColaboradorProjetoModel } from './colaborador-projeto.model';

describe('ColaboradorProjetoModel', () => {
  it('should be defined', () => {
    expect(new ColaboradorProjetoModel()).toBeDefined();
  });
});
